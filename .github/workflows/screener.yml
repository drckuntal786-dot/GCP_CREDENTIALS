name: Run Stock Screener

on:
  schedule:
    # 1. Pre-Market Scan: 9:08 AM IST (3:38 AM UTC) Mon-Fri
    - cron: '38 3 * * 1-5'
    # 2. Execution Run: 9:30 AM IST (4:00 AM UTC) Mon-Fri
    - cron: '0 4 * * 1-5'
    # 3. Nightly Reset / Preparation Run: 3:30 AM IST (10:00 PM UTC Sun-Thu)
    - cron: '0 22 * * 0-4'
  workflow_dispatch:

jobs:
  run-screener:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; else pip install pandas numpy yfinance gspread google-auth; fi

      - name: Setup Credentials
        env:
          GCP_CREDENTIALS: ${{ secrets.GCP_CREDENTIALS }}
        run: |
          echo "$GCP_CREDENTIALS" > service_account.json

      - name: Debug Workspace Directory
        run: ls -la

      - name: Set Run Phase Environment Variable
        id: set-phase
        run: |
          if [ "${{ github.event.schedule }}" == "38 3 * * 1-5" ]; then
            echo "RUN_MODE=PRE_MARKET" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" == "0 22 * * 0-4" ]; then
            echo "RUN_MODE=NIGHTLY_RESET" >> $GITHUB_ENV
          else
            echo "RUN_MODE=EXECUTION" >> $GITHUB_ENV
          fi

      - name: Run Screener
        env:
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          RUN_MODE: ${{ env.RUN_MODE }}
        run: python screener.py
